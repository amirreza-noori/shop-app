import { styles as productCardCompactStyles } from "./compact";
import { styles as productCardDefaultStyles } from "./default";

export const stylesList = {
	default: productCardDefaultStyles,
	compact: productCardCompactStyles,
};
